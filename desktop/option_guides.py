"""Reference cards for the GNU tools and bash shipped in the training image."""

_LS = '''ls 경로: 현재 위치를 바꾸지 않고 그 경로를 조회합니다.
-a / --all: 숨김 항목과 .(현재), ..(상위)까지 포함합니다.
-A / --almost-all: 숨김 항목은 포함하되 .과 ..는 제외합니다.
-l: 권한, 링크 수, 소유자, 그룹, 크기, 수정 시간, 이름을 자세히 표시합니다.
-h / --human-readable: -l과 함께 크기를 1024 단위 K/M/G 등으로 표시합니다.
-1 (숫자 1): 항목 하나당 한 줄. -l (소문자 L)과 다릅니다.
-R / --recursive: 하위 폴더도 재귀 조회합니다.
-r / --reverse: 정렬 순서를 뒤집습니다. 대문자 -R과 다릅니다.
-S: 크기 내림차순(큰 항목 먼저), -t: 수정 시간 내림차순(최신 먼저).
-d: 디렉터리 내용 대신 디렉터리 자체 정보를 표시합니다.
조합 예: ls -alh 경로 = ls -a -l -h 경로
비교 예: ls -t 경로 / ls -tr 경로 (최신 먼저 / 오래된 것 먼저)
비교 예: ls -ld 경로 / ls -l 경로 (폴더 자체 / 폴더 내용)
비교 예: ls -alR 경로 / ls -alr 경로 (재귀 조회 / 역순 정렬)
> 보고서: 표준 출력을 파일로 저장하며 기존 내용은 덮어씁니다.
화면의 열 배치와 파일로 저장한 열 배치는 다를 수 있습니다.
참고: ls --help. 옵션의 대소문자는 서로 바꿔 쓸 수 없습니다.'''

OPTION_GUIDES = {
    'mkdir': 'mkdir 폴더: 폴더 하나 만들기.\nmkdir -p 중간/폴더: 중간 폴더까지 만들기(뒤의 작업 폴더 단원에서 실습).\nmkdir -v 폴더: 생성 내용 표시.',
    'touch': 'touch 파일: 없으면 빈 파일 생성, 있으면 시간 갱신.\ntouch -c 파일: 없는 파일을 만들지 않음.\ntouch -m 파일: 수정 시간만 갱신. 내용을 비우는 명령은 아닙니다.',
    'read': 'cat 파일: 파일 내용 표시.\ncat -n 파일: 줄 번호 표시.\ncat -A 파일: 탭·줄 끝 등 숨은 문자 표시.\nls는 파일 목록이며 cat은 내용입니다.',
    'edit': 'nano 파일: nano 조작을 재현한 학습용 편집기 열기.\nCtrl+K: 현재 줄 잘라내기. Ctrl+U: 잘라낸 줄 붙이기.\nCtrl+O, Enter: 저장. Ctrl+X: 종료.\nCtrl+G: 편집기 도움말. ^ 표기는 Ctrl을 뜻합니다.\nF1/F2/F3/F5/F6은 Shellground 전용이므로 nano에서는 Ctrl 단축키를 사용하세요.\n편집한 내용은 가상 파일에만 저장되며 단원 전환 시 폐기됩니다.',
    'duplicate': 'cp 원본 대상: 복사.\ncp -i: 덮어쓰기 전 확인. cp -v: 처리 내용 표시.\ncp -r: 폴더 복사. cp -a: 속성·링크 등을 가능한 한 보존.\n처음에는 파일 하나를 복사하고 원본이 남는지 확인하세요.',
    'rename': 'mv 기존경로 새경로: 이동 또는 이름 변경.\nmv -i: 덮어쓰기 전 확인. mv -v: 처리 내용 표시.\n대상이 다른 폴더이면 그 폴더로 이동합니다. cp와 달리 원본 경로가 남지 않습니다.',
    'remove': 'rm 파일: 휴지통 없이 삭제.\nrm -i: 삭제 전 확인.\nrm -r: 폴더와 하위 내용 삭제. rm -f: 확인 없이 삭제하고 없는 파일 무시.\n처음 실습에서는 -r/-f를 사용하지 않고 지정한 파일 하나만 삭제합니다.',
    'permissions': 'chmod u+x: 소유자 실행 권한 추가.\nu: 소유자, g: 그룹, o: 기타, a: 전체.\n+x: 추가, -x: 제거. chmod 755: 소유자 rwx, 나머지 r-x.\nls -l로 전후 권한을 확인하세요. 실제 다운로드+실행은 뒤의 스크립트 단원에서 조합합니다.',
    'lsintro': _LS,
    'report': '명령 > 파일: 표준 출력을 파일로 저장하며 기존 내용을 덮어씁니다.\ncat 파일: 파일 내용을 표시합니다.\n비교: pwd / pwd > 보고서.txt / cat 보고서.txt.\n>>는 덧붙이기입니다. 지금 실습에서는 >만 사용합니다.',
    'mixed': '복습: pwd는 현재 위치, cd 경로는 이동입니다.\n새 옵션 없이 ls -a와 -l을 조합합니다.\n' + _LS,
    'navigate': '''pwd: 현재 셸의 위치. bash의 pwd -L은 논리 경로, pwd -P는 실제 경로입니다.
cd /절대/경로: 어디서든 같은 위치로 이동합니다.
cd 상대경로: 현재 폴더를 기준으로 이동합니다. cd ..: 상위, cd ~ 또는 cd: 홈.
.: 현재 폴더. ../guide.txt: 부모 폴더의 파일. docs/readme.txt: 하위 폴더의 파일.
같은 폴더에서 반복 작업할 때는 한 번 이동한 뒤 짧은 이름으로 작업합니다.
예: cd /home/learner/data → cd docs → cd .. (이동할 때마다 상대경로의 기준도 변경).
먼 폴더를 한 번 조회하거나 시작 위치와 무관해야 할 때는 절대경로가 편합니다.
cd -: 직전 작업 폴더로 돌아갑니다. cd -P 경로: 링크의 실제 경로로 이동합니다.
공백이 있는 경로: cd "daily notes". Tab으로 경로를 완성할 수 있습니다.
처음 비교할 명령: ls 경로 → ls -a 경로 → ls -l 경로 → ls -al 경로.
''' + _LS,
    'pwdpaths': '''pwd -L: $PWD가 유효한 경우 링크 이름을 유지한 논리 경로를 출력합니다.
pwd -P: 심볼릭 링크를 해석한 물리 경로를 출력합니다.
예: shortcut → /home/learner/data/release1234/docs 링크로 cd한 뒤
pwd -L에는 .../shortcut, pwd -P에는 .../release1234/docs가 나옵니다.
cd -L 경로: 논리 경로를 기준으로 처리합니다(기본값).
cd -P 경로: 링크를 해석한 실제 디렉터리를 기준으로 처리합니다.
cd ..: 상위로 이동. 링크에서 -L/-P에 따라 ..의 해석도 달라질 수 있습니다.
cd -: 이전 작업 폴더로 이동합니다.
주의: bash 내장 pwd와 /usr/bin/pwd는 기본 동작이 다를 수 있어 -L/-P를 명시하세요.
help pwd / help cd: 셸 내장 명령의 도움말입니다. pwd --help 대신 help pwd를 쓰세요.
이번 문제는 화면에 출력된 두 경로를 채점합니다.''',
    'list': _LS + '\n따라 비교하기: ls -1 경로 / ls -1a 경로 / ls -1A 경로. 숨김과 .·..의 차이를 보세요.\ncat -n 보고서: 줄 번호 표시. cat -A 보고서: 탭·줄 끝 등 숨은 문자 표시.',
    'long': _LS + '\n따라 비교하기: ls -al 경로 / ls -alh 경로. 크기 표시가 달라집니다.\n이 단원의 보고서는 바이트 단위(-h 없이) 상세 목록을 요구합니다.',
    'lsoptions': _LS + '\n이번 실습은 -A, -alh, -1S로 서로 다른 세 결과물을 만듭니다.\n이름 정렬과 크기 정렬은 다릅니다. -Sr은 작은 항목부터입니다.',
    'recursive': _LS + '\n보고서는 조회 대상 밖에 저장하세요. 내부에 만들면 보고서 자신도 목록에 포함됩니다.',
    'workspace': '''mkdir -p 경로: 없는 중간 폴더까지 만들고 기존 디렉터리는 오류로 취급하지 않습니다.
mkdir -v 경로: 생성 내용을 출력합니다. mkdir -pv 중간/새폴더처럼 조합 가능합니다.
touch 파일: 없으면 생성, 있으면 접근·수정 시간을 갱신합니다(내용을 지우지 않음).
touch -c 파일: 없는 파일을 새로 만들지 않습니다.
touch -m 파일: 수정 시간만 갱신. touch -a 파일: 접근 시간만 갱신.
비교: mkdir a/b / mkdir -p a/b (a가 없을 때 차이).
공백은 따옴표로: mkdir -p "daily notes"; touch "daily notes/done.txt".
확인: ls -ld "daily notes"; ls -l "daily notes/done.txt".''',
    'copy': '''cp 원본 대상: 파일 복사. 대상이 기존 폴더이면 그 안으로 복사합니다.
cp -r 원본폴더 대상폴더: 폴더 재귀 복사. cp -a: 가능한 속성·링크 등을 보존하는 복사.
cp -i: 덮어쓰기 전 확인. cp -v: 처리 내용 표시. cp -iv처럼 조합 가능합니다.
mv -i: 덮어쓰기 전 확인. mv -v: 이동·이름 변경 내용 표시.
rm -i: 삭제 전 확인. rm -r: 폴더와 하위 내용을 삭제. rm -f: 확인 없이 삭제, 없는 파일 무시.
안전 습관: 먼저 ls -ld 대상 확인 → 필요한 대상 하나만 수정. rm은 휴지통이 아닙니다.
이 단원 예시에 필요한 삭제는 파일 하나뿐이므로 rm -r/-f를 붙이지 마세요.
--는 옵션의 끝: rm -- -draft.txt처럼 -로 시작하는 파일명을 구분합니다.''',
    'grep': '''grep -i 패턴 파일: 대소문자 무시. -n: 줄 번호 표시. -v: 불일치 줄만.
grep -c: 일치한 줄 수. -w: 단어 단위. -E: 확장 정규식. -F: 패턴을 고정 문자열로 해석.
비교: grep error app.log / grep -i error app.log / grep -in error app.log.
grep -E 'error|warning' app.log: 두 패턴 중 하나가 있는 줄.
wc -l: 줄바꿈 수, -w: 단어 수, -c: 바이트 수, -m: 문자 수.
head -n 5 / tail -n 5: 앞/뒤 5줄. tail -f: 추가되는 내용 따라가기(Ctrl+C 종료).
이번 보고서는 원래 줄 내용만 요구하므로 -n으로 번호를 추가하면 통과하지 않습니다.''',
    'find': '''find 경로 -type f: 일반 파일. -type d: 디렉터리.
-name '*.deb': 이름 패턴(대소문자 구별). -iname '*.DEB': 대소문자 무시.
-maxdepth 1: 시작점과 바로 아래까지만 탐색. 이 문제에서는 깊은 경로도 필요합니다.
-size +1M: 1MiB보다 큰 파일. -mtime +7: 수정 후 지난 24시간 단위의 값이 7보다 큰 파일.
비교: find 경로 -type f -name '*.deb' / find 경로 -type d -name '*.deb'.
조건을 나란히 쓰면 기본적으로 AND. -o는 OR이며 괄호와 셸 인용에 주의하세요.
find의 숨김 폴더 탐색에는 ls의 -a 같은 옵션이 필요하지 않습니다.''',
    'curl': '''-o 파일: 저장 경로·이름 지정. -O: URL의 파일명으로 현재 폴더에 저장.
-L: HTTP 리다이렉트 따라가기. -f: HTTP 오류 응답을 실패로 처리.
-s: 진행 표시와 오류 메시지 숨김. -S: -s와 함께 오류는 표시.
-I: 본문 대신 응답 헤더 조회. 파일을 받는 -o와 혼동하지 마세요.
조합: curl -fL -o /저장/파일 URL. curl -fsS -o /저장/파일 URL.
이번 서버는 로컬 HTTP입니다. 파일 형식이 달라도 다운로드는 원본 바이트 저장입니다.''',
    'wget': '''-O 파일(대문자): 내용의 저장 이름 지정. -P 폴더: 저장 디렉터리 지정.
-o 파일(소문자): 다운로드 내용이 아니라 로그 파일 지정.
-c: 중단된 다운로드 이어받기 시도. 서버 지원과 기존 파일 상태에 따라 달라집니다.
-q: 조용히 실행. --spider: 다운로드하지 않고 링크 존재 확인.
비교: wget -O /저장/새이름 URL / wget -P /저장/폴더 URL.
curl -o와 wget -O는 같은 역할이지만 철자가 다릅니다.''',
    'archive': '''tar -t: 내용 목록. -x: 압축 묶음 해제. -c: 새 묶음 생성.
-z: gzip 처리. -f 파일: 아카이브 파일 지정. -v: 처리 파일 표시.
-C 폴더: 그 폴더로 이동해 작업(폴더를 먼저 만들어야 함).
비교: tar -tzf bundle.tar.gz / tar -xzf bundle.tar.gz -C unpacked.
생성 예: tar -czf backup.tar.gz 폴더. -f 뒤 파일 인자의 위치를 주의하세요.
unzip -l 파일: 내용 조회. unzip 파일 -d 폴더: 지정 위치로 풀기.
unzip -n: 기존 파일 덮어쓰지 않기. -o: 확인 없이 덮어쓰기(주의).''',
    'script': '''chmod u+x 파일: 소유자에게 실행 권한 추가. g: 그룹, o: 기타, a: 전체.
chmod u-x 파일: 소유자의 실행 권한 제거. chmod go-w 파일: 그룹·기타 쓰기 제거.
chmod 755 파일: 소유자 rwx, 그룹·기타 r-x. 숫자는 r=4, w=2, x=1의 합.
ls -l 파일로 권한 확인. 이번 실습에서는 u+x로 필요한 권한만 추가하세요.
./파일.sh 인자: 현재 폴더의 스크립트를 직접 실행(실행 권한 필요).
bash 파일.sh 인자: bash가 파일을 읽어 실행. 이번 문제는 실행 권한도 별도 검사합니다.
명령을 받기 전에 cat -n 파일로 내용을 확인하세요.''',
    'deb': '''dpkg-deb -I / --info 파일: 패키지 정보. -c / --contents 파일: 포함 파일 목록.
-x / --extract 파일 폴더: 파일 내용만 추출(설치 아님).
-e / --control 파일 폴더: 제어 정보 추출. -f / --field 파일 Package: 특정 필드 조회.
비교: dpkg-deb -I toolkit.deb / dpkg-deb -c toolkit.deb.
dpkg-deb -x toolkit.deb unpacked 후 ls -R unpacked로 구조를 확인하세요.
dpkg -i는 시스템 설치로 의미가 다르며 이 실습에서는 사용하지 않습니다.''',
}
