# 검증 범위

## 4.7.3-dev Linux pip·관련 실습 검토판 (2026-09-17)

- 새 파일 `dist-linux-pip-review/Shellground`, SHA-256 `2e5fdcf539802a257466d6b686a8a41a59e24592876dea8af0bf832b39503735`. 기존4.7.2와 두 Python 실행파일의 SHA는 변경되지 않았다. runtime은 기존 immutable 기본이미지를 hardlink하고 새 검증 metadata만 별도 inode로 만들었다.
- 실제 guest에서 Conda·pip 60문제(1624.07초),20학습 시퀀스,설치12검사(33.64초),기존 오답복구4건·답안변형13건·새 pip 상태31건 PASS. `.conda-build/pip-full/`의 보고서는 기본이미지SHA `92ad90b561abe6ac7851244cb2abadd0836e3b328216b8b72ac5ecc3fb81b7fc`,course `a94a1c17152ea47ae650fcc7cad655320bb8c243ac29e020b327fc5aa06a602f`,learning `780fa49d4902d8145650edd84361f1ebaccb3fd042350517292b0bbb46703dd9`,setup `afc7039c855bbced3951fe6aaf44fd0978bc5b1da1a4b9c019e86754e1668054`에 묶여 있다. VM 종료·임시 디스크 제거 true를 확인한 뒤에만 새 팩을 만들었다.
- 일반 회귀450개 중390실행PASS/60선택검사 제외(55.666초). 관련UI39개PASS, 문구 수정 후 관련8개와 단일창 진단 재통과. 선택검사를 통과로 세지 않는다.
- **빌드된 새 파일 자체**에서 Python91항목273문제+98오답/동등 풀이 PASS(12.829초),한창·Shift+Enter·F6·퀴즈→pip·입력/프로세스 보존·숨은Python 종료 PASS. 실제Linux/ROS/화면/APT/Docker갱신·복원·배포/계정권한/복사9280 진단 PASS(기동7.53초,종료0.17초).
- 새 파일의 Conda 실제 실습/같은PTY 재시도/YAML 읽기/VM 정리 PASS38.28초; 실제 Miniconda 설치UI10흐름 PASS41.67초; 실제 pip 질문n/y·오답 후 재시도·F5·예시와 단원 완료 분리·소단계 재개·숨은VM 종료11흐름 PASS77.34초.
- `dist-linux-pip-review/verification` 캡처7장을 시각 확인했다. 한글·터미널 표시와 설치6항목/pip4항목의 한 화면 표시를 확인했다. quiz-related-practice 캡처는 검토한 소스 캡처와 SHA도 동일하다.
- 빌드/VM 검사와 절전 방지 프로세스는 종료됐다. Windows·Android·macOS 포팅, 실제 Jupyter 커널 선택, Linux·Docker·ROS의 모든 변형 및 전체 PDF 범위 완료를 뜻하지 않는다. 다음 범위와 실행 방법은 [검토 안내](LINUX_PIP_REVIEW.md)에 구분했다.

## 4.6.0-dev 실제 Linux 과정·소단계 저장 (2026-09-16)

- Linux 60/Docker15/ROS20의 95단원과 19복습, 기존 모든 단원 키/내용 보존을 검사했다. 새 Linux 25단원과 새 복습에는 자동 완료를 부여하지 않는다. 두 seed×세 변형의 풀이·소단계·복습에서 명령 선행 학습 순서, 초반 리다이렉션/파이프 제한, useradd 옵션 누적 순서를 검사했다.
- 일반 회귀 **245개 중 188통과/57건너뜀, 21.635초**. 별도 실제환경 검사는 아래와 같이 따로 실행했다. 신규 소단계 저장 검사는 실제/모의 모드 독립, 종료 후 위치/확인 복원, 입력/seed 미저장, 마지막 소단계가 숙달 완료로 오인되지 않음, 단계 제목 재배치, 기존/미래 기록 보존을 포함한다.
- `SHELLGROUND_COURSE_LIVE=1` 전용 KVM에서 새25단원×예시/활용1/활용2 **75시나리오 모두 초기 미완료→실제 Bash 명령→실제 상태 채점 통과**, 64.541초. 계정 생성·변경은 실제 useradd/usermod로 실행했다.
- 새 Linux 12종합 복습과 초반 list/recursive 활용2도 실제 PTY로 해결해 모두 통과했다. nano는 실제 편집기에 Ctrl+K/입력/Ctrl+O/Enter/Ctrl+X를 보냈다. 두 파일 원본 보존·공백 경로·권한·계정 인계 조합 포함.
- 실제 Linux 소단계 풀이 순서 검증에서 deb의 추출 목적지 불일치를 발견했다. 목표인 unpacked로 수정 후 모든 작성된 Linux 소단계의 최종 목표 통과, **41.477초**. 검증 프로세스는 자신이 만든 임시VM만 정상 종료했다. 사용자의 앱·실습VM·개인 진도는 수정하지 않았다.
- 채점기만 검증된 게스트 임시 디스크에 전달한다. 호스트 셸에서 학습자 입력을 실행하거나 자작 시뮬레이터 엔진을 추가하지 않았다. 이 검증은 전체 강의 범위 완료나 Windows 실기기 검증이 아니다.
- Linux 실행 파일 4.6.0-dev 재빌드·번들 자체 검사·버전 확인 완료. [소단계 재개 화면](ui-previews/learning-resume-46.png)을 offscreen으로 렌더해 확인했다. 저장된 소단계2/2·확인1/2, 재개 준비 버튼, 기존 A배치의 터미널968×299px을 유지한다. 사용자 앱은 강제로 재시작하지 않았다.
- 최종 번들 `--self-test-real`도 통과: 실제 ROS·화면·APT·Docker 이미지 갱신/아카이브복원/배포·계정권한·copy9280 검사 모두 성공. 기동2.82초, 종료0.11초. 임시프로필을 사용한 번들 실제모드 UI 기동도 오류 없이 확인한 뒤 검사 프로세스만 5초 후 종료했다. 실행파일 크기84,855,776바이트. 실행 중인 사용자 실습과 진도 파일은 변경하지 않았다.

## 4.5.0-dev 설정·올랜덤 종료·Linux14/15 지시문 (2026-09-16)

- 설정 창: 라이트/다크 프로그램 테마, 독립 터미널 배경, ANSI 색상 전체/8계열 선택, 터미널·설명 글자 크기, 드래그 자동 스크롤. 미리보기/취소/기본값/저장 실패 동작을 검사했다. `settings-v1.json` 별도 원자적 저장, 두 모드 설정 공유와 진도 파일 불변을 확인했다.
- 색상 변경은 VT 원문/셀 속성/셸 입력/연결/선택/기록을 초기화하지 않고 표시만 바꾼다. 편집기 반전 속성 유지, RGB/밝은 색/기본색 처리, 새 터미널에도 설정 적용, 글자 크기에 따른 resize, 자동 스크롤 중지 검사를 포함한다.
- 올랜덤 범위 표시와 전용 종료 버튼: 미완료/통과/시작 실패 상태에서도 이전 단원 배우기 또는 복습 대기로 복귀한다. 거절/준비 중/범위 전환/직접 다른 단원 선택 경로와 완료 진도 보존 검사. 이전 임시 실습 상태 복원을 약속하지 않는다.
- Linux14 활용2 seed5942: `.where`에 이동 후 현재 디렉터리의 절대경로 한 줄 저장을 명시. Linux15 seed8372: 예시에서 목표에 없는 앞뒤 `pwd` 제거, 설명에서 선택적 위치 확인임을 구분. 활용2 seed8133: 실제 원본 `source/guide.txt`와 새 `target/backup` 생성·복사 지시를 명시하고 원본 보존 채점도 추가.
- 일반 회귀 **234개 중 180 통과 / 별도 환경 54 건너뜀(21.722초)**. 추가 실제 KVM 검사 1개에서 위 3문제를 실제 Bash PTY로 풀고 모두 채점 통과(4.601초). seed8133의 초기 원본 존재/backup 부재, 생성 후 백업 내용, 원본 훼손 시 실패까지 확인했다. 임시 검증 VM은 종료했고 사용자 앱/진도는 변경하지 않았다.
- 1220×880 A 배치 다크 화면의 7개 평가 항목 및 610×700 설정 창을 offscreen 렌더해 한글/대비/버튼/미리보기 표시 확인. Windows 실기기 검증과 ROS 전체 범위 검증을 의미하지 않는다.
- Linux 실행 파일 `dist/Shellground` 4.5.0-dev 최종 빌드 및 번들 자체 검사 완료(빌드73.212초). 런타임 기본 이미지는 그대로 재사용했고 사용자 앱을 강제로 재시작하지 않았다. 이번 작업에서 자작 시뮬레이터 엔진 파일은 수정하지 않았다. 최신 지시대로 앞으로 모의 엔진의 수정·확장은 중단하고 실제 Linux 문제·설명·채점·UI에 집중한다.

## 4.4.0-dev 소단계·복구 기준 명시·터미널 드래그 (2026-09-16)

- Linux 실행 파일 `dist/Shellground` 4.4.0-dev 재빌드 및 번들 자체 검사 완료. 이전 runtime 팩을 그대로 사용하고 실행 중인 사용자 앱은 재시작하지 않았다. Windows 실행 파일/실기기 검증은 이번 작업 범위에서 하지 않았다.
- 사용자 선택대로 큰 번호/완료 키는 유지하고 Docker 15개·ROS 20개를 각 3~6개 설명+실습 소단계로 구성했다. 전체 해설/옵션은 F3에 남기고 현재 단계만 학습 화면에 표시한다. 마지막에는 기존 조합 예시와 활용 1·2를 유지한다.
- 소단계 F6/이전 이동 시 mission 객체·실습 파일·셸·화면 버퍼·기록·세션 세대를 보존한다. F2 초기화는 1부터, 마지막 소단계 다음은 새 전체 예시로 이동한다. 단순 페이지 열람으로 완료를 저장하지 않는다. 모의 Docker 10단원의 소단계 명령을 순서 실행하고 원래 상태 채점 통과를 검사했다.
- 실제 KVM 별도 게스트에서 Docker limits/bind/volume/network/build/diagnose의 소단계 순서와 원래 목표 채점 통과(40.769초). read-only 입력, 숫자 권한, 볼륨 백업/새 저장소 복원, 네트워크 추가/분리, 점진적 Dockerfile/ENV, 실패 로그/설정 복구를 포함한다. 검증 VM을 정리하고 사용자 VM은 유지했다.
- ROS 파라미터 소단계 및 Docker 메모리 소단계를 1220×880 A 배치로 렌더해 육안 확인했다. 두 문제 화면 모두 수직 스크롤 최대값 0. 테스트/채점 탭에서는 소단계 내비게이션을 숨긴다. 학습 소단계 전체가 모든 창 크기에서 무스크롤이라는 뜻은 아니다.
- Linux12 활용2 seed3504의 guide.txt 원본/손상된 manual.txt/기존 draft.txt를 모의 및 실제 KVM에서 재현 확인했다. 실제 풀이 후 원본 보존·복구·초안 백업·이름 변경·삭제 채점 통과(3.784초). “원본으로 복구” 대신 기준 파일 전체 경로와 이름이 다른 대상 덮어쓰기를 명시하고 중복 지시를 제거했다.
- 터미널 선택을 화면 좌표가 아닌 전체 기록의 절대 행 좌표로 관리한다. 버튼을 누른 채 위/아래 가장자리에서만 75ms 타이머로 1~6줄 자동 스크롤한다. 버튼 놓기/내부 복귀/포커스 이탈/숨김/초기화/끝 경계에서는 타이머를 멈춘다. Ctrl+Shift+A 및 우클릭으로 현재 보관 기록 전체 선택도 제공한다.
- 터미널 새 검사 6개: 양방향/역방향/정지 포인터 드래그, 화면 밖 기록 복사, 새 출력 중 선택 유지, 종료 조건/유휴 타이머 정지, 한글 전체 선택, 3,000줄 한도 초과 처리. 기존 스크롤 위치/입력 시 최신 이동 검사도 유지.
- 최종 전체 회귀 **217개 중 164 통과 / 별도 환경 검사 53 건너뜀(19.983초)**. 모든 일반 검사가 통과했다. 실제 ROS 전체 변형/복습 및 Windows 실제 환경 검증 완료로 확대하지 않는다. 게스트 이미지는 바꾸지 않았으며 낮은 우선순위로 검사/빌드했다.

## 4.3.2-dev ROS 설명 보강 (2026-09-16)

- ROS 20단원의 기존 요약을 보존하고 목적/인자/옵션/확인/실수, 새 터미널과 파일 인계 해설을 배우기/F3에 연결했다. Humble 공식 문서/CLI 소스 대조 링크는 ROS_TEACHING.md에 기록했다.
- 새 회귀 6개: 모든 설명과 F3 연결, 활용 2의 사전 설명, 시험에 해설 미노출, echo/record 중단·터미널 전환, 실제 Bash/find/cp 인계, 20단원의 Qt 배우기/F3/활용 화면. 임시 프로필만 사용했다.
- 전체 검사 203개 중 152개 통과, 별도 실제 환경용 51개 건너뜀(18.924초). 사용자 앱/실습을 종료하거나 완료 기록을 수정하지 않았다. 게스트 이미지/채점 규칙은 바꾸지 않아 추가 VM을 띄우지 않았다. 이번 결과는 ROS 전체 실제 실행 재검증이 아니다.
- Docker·ROS 단원 밀도가 높다는 추가 피드백은 별도의 미완료 과제로 기록했다. 번호/완료 기록까지 분리할지, 내부 소단계로 나눌지 사용자에게 질문했다. 설명 보강을 단원 재편 완료라고 표시하지 않는다.

## 4.3.1-dev Docker 설명·이미지 주소 누락 수정 (2026-09-16)

- Linux 최종 실행 파일 `dist/Shellground` 4.3.1-dev 빌드 및 번들 자체 검사 통과. 내장 이미지는 실제 검증한 4.3.0 팩을 그대로 사용한다. 진행 중인 사용자의 문제나 완료 기록을 강제 초기화하지 않았다.
- 실제 모드 문제의 `ubuntu:24.04와`에서 Unicode 단어 경계 때문에 내장 레지스트리 주소로 치환되지 않던 오류를 수정했다. 사용자가 제공한 images/seed5131을 재현하고 와·과·로·를·은·의·에서, 반복 변환, 이미 한정된 참조, 더 긴 다른 태그를 회귀 검사한다. 설명용 일반 Docker Hub 참조와 실행 예시는 구분한다.
- Docker 15단원 전체에 옵션 위치/역할, 상태 변화, 확인 방법과 대표 실수를 추가했다. 기존 배우기 화면과 F3 설명에서 함께 보이며, 실습/채점 조건을 줄이지 않았다. `localhost`의 기준, 앱이 고른 포트 5000, 파일 폴더가 아닌 training 이름 공간, 왜 오프라인 교육 저장소를 쓰는지 실제 모드 첫 단원에 설명한다.
- 추가 조사 목표가 있는 기존 Docker 10단원에는 전체 목록의 -a, 셸 리다이렉션 >와 >> 차이, 보고서 저장 위치, cat 확인, 작업 후 최신 상태로 다시 저장하는 이유를 명시했다. 목표 문제에 해설을 주입하지 않는 검사도 추가했다.
- 전체 회귀 196개 중 145개 통과/51개 별도 환경 검사 건너뜀(17.615초). 이후 모든 기존 Docker 문제/풀이의 교육용 주소 검사를 1개 추가했고 관련 설명·주소 검사 8개가 통과했다. 설명상의 목표와 기존 보고서 채점의 모든 경계 조건 검증은 구분한다.
- 참고한 1차 자료: [pull](https://docs.docker.com/reference/cli/docker/image/pull/), [run](https://docs.docker.com/reference/cli/docker/container/run/), [exec](https://docs.docker.com/reference/cli/docker/container/exec/), [stop](https://docs.docker.com/reference/cli/docker/container/stop/), [tag](https://docs.docker.com/reference/cli/docker/image/tag/), [commit](https://docs.docker.com/reference/cli/docker/container/commit/), [save](https://docs.docker.com/reference/cli/docker/image/save/), [volumes](https://docs.docker.com/engine/storage/volumes/), [bind](https://docs.docker.com/engine/storage/bind-mounts/), [bridge](https://docs.docker.com/engine/network/drivers/bridge/), [Dockerfile](https://docs.docker.com/reference/dockerfile/). 설명은 독자적으로 작성했으며 실제 실습 증거는 아래와 구분한다.

## 4.3.0-dev 실제 Docker 심화 11–15 (2026-09-16)

- 추가 신고: 리눅스 12 활용1의 seed 9280에서 사용자가 원본 폴더를 조회한 기록을 제공했다. 모의 환경 재현 시 `draft.txt`는 `/home/learner/work/output/result9280`에 있었고 원본 `/home/learner/archive/staging/release9280`에는 없었다. 초기 파일 생성을 임의 변경하지 않고 문제에 두 폴더의 전체 경로를 따로 표시하고 `final.txt`의 대상 경로를 명확히 했다. 원본 폴더로 이동한 상태에서 절대경로로 해결하는 회귀 검사와 번들 실제 검사에 같은 seed를 추가했다.
- 위 신고 반영 후 최종 일반 회귀는 191개 중 140개 통과/51개 건너뜀(17.466초)이다. 아래 190개 기록은 직전 Docker 확장 검사다.
- 실제 모드 70단원·14복습, Docker 표시 01–15. 기존 65개 단원 키/ROS·계정 복습 키와 모의 모드 45단원·9복습을 보존했다. 새 저장 키는 `docker_bind/volume/network/build/diagnose`, 복습은 `docker-checkpoint-deployment`다.
- 일반 회귀 검사 190개 중 139개 통과, 별도 환경용 51개 건너뜀(18.736초). 기존 사용자 진도를 읽어 고치는 테스트가 아니라 임시 프로필을 사용한다. 번호 연속성, 잠금 없음, 문제의 F키 안내 제거, 7개 평가 표시, 검사 프로브 시간초과 정리·기존 자원 보존도 포함한다.
- 별도의 오프라인 KVM 개발 게스트에서 `test_live_docker` 8개 통과(131.080초). 5단원×예시/활용1/활용2 전부 실제 Docker로 실행해 최초 미완료→목표 달성 통과를 확인했다. 새 종합 복습은 Bash PTY에 명령을 한 줄씩 입력해 통과했다.
- `-v` 대안 정답, 입력 rw/출력 ro 오류, 기존 컨테이너 재생성 거부, 원본 볼륨 데이터 훼손, 보호 네트워크 분리, 보고서만 위조한 미빌드 이미지, 실제 숫자 UID/GID가 같은 이름 기반 USER와 잘못된 GID 구분, 실패 후 같은 실습 복구·재채점, 초기화 뒤 실습 자원 제거를 검사했다.
- 이미지 설정만 읽지 않고 자원 제한(0.5 CPU·64MiB·64 PID·8초)의 새 컨테이너에서 기본 명령 출력과 빌드 결과물을 검사한다. 검사용 프로브는 성공/실패/시간초과 후 제거한다. 학생 컨테이너나 파일은 채점 때문에 재생성하지 않는다.
- 동일 실제 게스트에 앱의 `configure_shell()` 경로로 bashrc를 전달해 초록색 프롬프트·파란 폴더와 평문 파일 저장을 확인하고 실제 VT 렌더를 육안 확인했다. 4.2.3 때 남겨 둔 색상 설정 전달의 게스트 검사를 보완했다.
- Docker 29.1.3의 번들 환경에서 Dockerfile 실제 빌드가 통과했다. 추가 호스트 Docker/WSL/buildx 설치는 하지 않았다. 개발 VM은 KVM·4vCPU·2GiB·nice 10이며 사용자 실습 VM을 중단하지 않았다. Windows 실기기와 나머지 강의 범위 검증은 아직 별도 미완료다.
- 개발 VM을 정상 정리·종료한 뒤 기본 이미지 내보내기/무결성 검사 및 Linux 실행 파일 재빌드를 완료했다. 기반 이미지는 4,125,360,128바이트이며 이전 팩을 보존했다. 최종 4.3.0 실행 파일의 `--self-test-real`에서도 ROS/화면/APT/이미지 갱신/save-load/계정권한/Docker 종합 배포 및 신고한 copy9280 초기 파일·풀이·채점이 모두 통과했다. 기동 4.38초, 종료 0.17초. 이 실제 검사는 4.3.1 설명 보강 직전에 수행했다.

## 4.2.3-dev 실제 터미널 색상 기본 활성화 (2026-09-16)

- 게스트 bashrc에서 초록색 사용자 프롬프트, 파란색 경로 및 `dircolors`, `ls --color=auto`, `grep --color=auto`를 설정한다. PS1을 환경변수에만 맡기지 않고 대화형 Bash 시작 파일에서 지정한다.
- 앱에 bashrc를 포함하고 게스트 확인 뒤 첫 터미널을 열기 전에 전용 게스트의 `/opt/shellground/bashrc`만 갱신한다. 게스트 소유 마커·virtio 포트 검사를 통과해야 하며 호스트 설정·기존 완료 기록·읽기 전용 기본 이미지는 변경하지 않는다.
- 로컬 실제 Bash/coreutils PTY로 초록색 프롬프트·실행 파일, 파란색 디렉터리, 검색 강조 및 리다이렉션 파일에 ANSI 코드가 없음을 확인했다. VT 화면 파서의 색상/굵기 및 일반 텍스트 복귀도 검사했다.
- 전체 회귀 검사 176개 중 133개 통과, 실제 게스트 등 별도 환경 검사가 필요한 43개 건너뜀(17.461초). 사용자가 실행 중인 VM을 종료하거나 추가 검증 VM을 띄우지 않았다. 이번 변경의 게스트 기동부터 종료까지 실제 통합 검사는 아직 별도 미실행이다.
- Linux `dist/Shellground` 4.2.3-dev 재빌드 및 번들 자체 검사 통과. 자체 검사에서 bashrc 리소스 포함도 확인한다. 기본 이미지의 불필요한 복사/변환 없이 앱만 갱신했다.

## 4.2.2-dev 선행 잠금 제거·모드별 진도 표시 (2026-09-16)

- 사용자 요청으로 미완료 단원과 종합 복습의 선행 완료 잠금을 제거했다. 없는 인덱스는 거부하지만 지원되는 모든 단원/복습은 완료 0개 상태에서도 직접 선택된다. 단순히 열어 본 단원을 자동 완료로 만들지는 않는다.
- 일반 회귀 검사 172개 중 129개 통과, 별도 실제 환경이 필요한 43개 건너뜀(14.691초). 독립적으로 완료한 복습이 재실행 후 유지되는지, 01–09 완료 기록에서 복습을 강제하지 않고 10단원으로 진입하는지, 다른 모드 기록을 생성/복사하지 않는지 포함한다.
- 모드 선택 창에서 실제/시뮬레이션 각각의 저장된 단원·복습 완료 개수를 표시한다. 해당 모드에 파일이 없으면 다른 모드의 기록은 유지된다는 안내를 표시한다. 읽기 오류를 0개 완료로 위장하지 않는다.
- 실제 사용자 데이터의 기존 모의 기록이 남아 있음을 확인했고, 요청된 범위만 실제 모드에 별도 완료 처리했다. 기존 파일·추가 완료 단원·기억노트는 지우지 않았다. 이는 사용자 요청에 따른 표시 복구이지 실제 명령 시험을 통과했다는 검증 근거가 아니다. 하드코딩한 사용자 완료 목록은 프로그램 소스에 넣지 않았다.
- Docker 자료 대조 중 켰던 전용 개발 VM은 요청 전환 직후 정상 종료했다. 이번 UI/진도 검사는 VM 없이 수행했다.
- Linux `dist/Shellground`를 4.2.2-dev로 재빌드하고 번들 자체 검사 통과를 확인했다. 실행 중인 사용자의 앱을 강제 종료하지 않았으므로 새 UI/진도 표시는 재실행 후 적용된다.

## 4.2.1-dev A 채택·문제/결과 탭 (2026-09-16)

- 사용자가 A안을 선택한 뒤 기본 레이아웃으로 적용했다. 별도 하단 평가 패널은 제거하고 ‘문제 / 채점 결과’가 같은 공간을 사용하도록 했다.
- 일반 회귀 검사 168개 중 125개 통과, 별도 게스트 환경 등을 요구하는 43개 건너뜀(10.709초). 화면을 켜거나 VM을 실행하지 않고 검사했다.
- 새 UI 검사는 1220×880 및 1100×760에서 7개 평가 항목의 수직/수평 스크롤 최대값이 0인지 검사한다. 채점 자동 전환, 문제 탭 복귀, 통과 후 결과 탭에서 F6, 터미널 크기/행/열/기록 및 mission 객체 보존을 확인했다. 40개처럼 많은 결과는 탭 내부 스크롤로 유지하며 터미널 영역을 침범하지 않는다.
- 두 모드의 모든 단원/활용 변형(각 2개 seed)과 종합 복습으로 생성한 682개 문제 본문에 F1–F8·채점·힌트·버튼 안내가 없음을 확인했다. 편집기의 학습용 조작 설명은 예시에 남기되 F5 안내는 제거했다. 작업 종료 위치나 ROS 발행 중단처럼 실제 평가되는 목표 조건은 유지했다.
- 오프스크린 렌더를 직접 확인했다. 문제/결과 각각 968×224px, 터미널 968×298px이며 탭 전환 전후 터미널 크기가 같다. [문제](ui-previews/adopted-a-question.png) / [7항목 결과](ui-previews/adopted-a-results.png).
- Linux 실행 파일을 재빌드하고 `--version`=4.2.1-dev와 번들 `--self-test` 통과를 확인했다(84,737,920바이트). 번들의 `--layout-preview --layout a`도 offscreen으로 기동해 오류 없이 실행됐으며 검사 소유 timeout이 4초 뒤 종료했다. 실제 VM 시작 검사는 아니다.
- 전체 강의 범위·Windows 실기기·최신 전체 실제 런타임 검사는 여전히 별도 미완료다. 이번 UI 테스트 결과를 실제 Linux/Docker/ROS 전체 검증으로 확대 해석하지 않는다.

## 4.2.0-dev 계정 과정·분야 분리·UI 시안 (2026-09-16)

- 계정/그룹/권한 5단원×3변형과 종합 복습을 실제 Linux 게스트에서 검사한 회귀 5개 통과(12.511초). 기존 그룹 손실, 잘못된 UID, 홈 누락, 이어서 복구·재채점과 모의/실제 파일 접근 권한 대조를 포함했다. 전체 강의 범위 완료를 뜻하지 않는다.
- UI/분야 분리 후 일반 회귀 검사 166개 중 123개 통과, 별도 실제 게스트 등 43개 건너뜀(10.726초). 처음 실행의 import 경로 누락과 샌드박스 로컬 소켓 차단을 해결한 동일 범위 재실행 결과다.
- 분야 번호는 리눅스/Docker/ROS 2별로 01부터 표시하고 저장용 키는 유지한다. 분야별 진도 복원·독립 시작·복습 연결·평가 중 명령어 숨김·분야별/전체 랜덤을 검사했다.
- UI 시안은 기존/A/B/C를 같은 1220×880에서 렌더하고 확인했다. 시안 비교에서 실행 엔진을 생성하지 않는지와 임시 진도 폴더가 비어 있는지 검사했다. 상세 수치는 [UI_LAYOUT_REVIEW.md](UI_LAYOUT_REVIEW.md).
- 이번 UI 검토에서는 실제 VM을 기동하지 않았다. 최신 4.2 실행 파일의 전체 실제 런타임 검사는 별도로 남아 있으며 이전 4.1.2 결과와 구분한다. Windows 실기기 검증도 남아 있다.

## 4.1.2-dev 앱 비정상 종료와 전용 VM 수명 관리 (2026-09-15)

- 최종 일반 발견 검사 147개 중 109개 통과, 명시적 실제 환경 검사 38개 건너뜀(9.519초). 실제 프로세스 강제 종료·파이프·잠금·생성 시각 확인·다른 실습 보존 검사는 이 통과 수에 포함한다. 건너뜀을 Windows 검증으로 세지 않는다. 반복 실행에서 드러난 procfs 읽기 도중 프로세스 소멸(`ESRCH`) 경합도 별도 회귀 검사로 추가했다.
- 별도 소스 실행 검사에서 KVM 기반 ROS 실제 채점·화면·정상 종료 통과. 제어 채널 준비 1.61초, 종료 0.11초, 전체 14.87초.
- Linux 실행 파일 `--self-test-real-crash`에서 시작 중(`guest_ready=false`)과 준비 완료 후(`guest_ready=true`) 임시 앱이 `os._exit(86)`으로 종료되도록 했다. 두 경우 모두 실제 VM·관리 프로세스가 멈추고 전용 임시 디스크가 사라졌다. 기반 이미지 크기·수정 시각은 그대로다. 관찰 프로세스가 임시 앱 종료를 확인한 뒤 정리 완료를 관측하기까지 각각 0.00/0.05초였으며, 실제 종료 시점부터의 정밀 측정은 아니다.
- 소스 프로세스 검사에는 관리 프로세스 자체를 SIGKILL했을 때 자식이 실행 상태로 남지 않는지, 남은 디스크를 잠금·표식 확인 후 회수하는지, VM 생성 전 앱 종료 디스크도 다음 실행에서 회수하는지 포함한다. 실제 QEMU를 켠 별도 검사에서도 관리 프로세스의 Popen 핸들을 강제 종료한 뒤 QEMU 종료·남은 디스크 회수·기반 이미지 불변을 확인했다(1.548초). Windows 실기기 검증은 남아 있다.
- 최종 실행 파일의 `--self-test-real`도 실제 ROS·화면·APT·Docker 갱신/저장/복원 검사에 통과했다. 제어 채널 준비 2.43초, 정상 종료 0.11초. 검사 후 QEMU 또는 관리 프로세스가 남아 있지 않음을 확인했다.
- 경합 보완까지 반영한 최종 Linux 실행 파일 크기는 84,703,400바이트다. 기반 이미지 내용은 바꾸지 않았다.

관리 프로세스는 Qt 없이 시작하고, Linux 자식 프로세스의 부모 종료 신호를 설정한 뒤에만 파이프 감시 스레드를 시작한다. Windows 코드는 [Microsoft Job Objects 문서](https://learn.microsoft.com/en-us/windows/win32/procthread/job-objects)의 자식 포함·kill-on-close 정책을 사용하되 설정 실패 시 VM을 시작하지 않는다. 이 Windows 경로는 문서 대조이며 실기기 검증이 아니다. 단일 실행 파일의 관리 프로세스는 [PyInstaller의 독립 환경 실행](https://pyinstaller.org/en/stable/advanced-topics.html#envvar-PYINSTALLER_RESET_ENVIRONMENT)을 사용해 원래 앱의 임시 라이브러리 폴더가 정리돼도 종료 처리를 계속할 수 있게 했다.

## 4.1.1-dev 실제 Linux 확장과 자원 회귀 (2026-09-15)

Linux 실행 파일과 내장 런타임 팩을 재빌드했다. 아래 기록은 전체 제품 또는 Windows 지원 완료를 뜻하지 않는다. 이전 버전의 기록은 당시 상태로 보존한다.

- 일반 발견 검사: 132개 중 96개 통과, 명시적 실제 환경이 필요한 36개는 건너뜀(6.327초).
- 별도 앱 전용 VM의 실제 확장 검사 7개 통과: 기존 27–40단원, 30/35/40 종합 복습, 긴 제어 응답, pull만 한 상태와 save만 한 상태의 오채점 방지. 모든 seed·변형 조합을 검사한 것은 아니다.
- ROS 준비 상태 수정 후 실제 파라미터 dump/load와 colcon overlay 검사 2개 통과(15.929초). 앞서 검사한 ROS 기본 범위와 구별하며 전체 활용·복습 검증은 남아 있다.
- 패키징된 `dist/Shellground --self-test-real` 통과: 네트워크 장치 없이 실제 ROS 파라미터/PNG 화면, 로컬 APT 설치, Docker 이미지 갱신과 save/remove/load, 초기 미완료와 풀이 후 완료 판정. 제어 채널 준비 1.55초, 종료 0.04초. 이는 전체 학습 화면 로딩 시간이 아니다.
- 실행 파일 84,617,944바이트, 기반 qcow2 이미지 4,111,794,176바이트. 실행 파일 옆 runtime 폴더를 함께 유지해야 한다.
- 최종 검사 후 개발/검증 QEMU 프로세스가 남아 있지 않음을 확인했다. 이전 개발용 기반 이미지는 `.vm-build/pack-backups`에 보존했다.
- 지속 CPU 원인과 스레드별 재측정은 [RESOURCE_VERIFICATION.md](RESOURCE_VERIFICATION.md)에 기록했다.

Windows 실행 파일·실기기, 강제 종료 뒤 복구, 전체 강의의 심화 옵션과 모든 과제 변형은 미완료다.

## 4.0.0 오프라인 상태 시뮬레이터 (2026-09-15)

실제 Docker 실행 백엔드 대신 메모리 내부 POSIX 파일/셸·작업·패키지·Docker 상태 모델로 전환했습니다. 사용자 입력을 호스트 셸에 실행하지 않습니다. Linux 빌드에서 검증하며 Windows 실행 파일 빌드·실기기 검증은 수행하지 않았습니다.

Linux 4.0.0 단일 실행 파일을 재빌드했습니다. 패키징된 실행 파일의 자체 검사에서 가상 폴더 생성과 Docker commit→새 컨테이너 파일 복원 과제를 직접 실행·채점해 통과했습니다. 외부 Docker 데몬이나 네트워크는 사용하지 않았습니다.

- 현재 앱을 대상으로 한 **54개 테스트 모두 통과**, skip 없음(약 8초).
- 전체 발견 검사 **84개 중 69개 통과, 이전 실제 Docker 백엔드 통합 검사 15개 건너뜀**. 이전 문자열 퀴즈·Docker 백엔드는 보관용이며 현재 GUI 경로가 아닙니다.
- 40단원 × 3개 seed × 3개 변형 = 360개 과제의 초기 미완료와 풀이 후 통과를 검사. 8개 종합 복습도 별도 검사. 이 조합 수는 테스트 메서드 개수와 다릅니다.
- 가상 FS 경로·파일·링크·권한, 따옴표·환경변수·파이프·리다이렉션·종료 코드, nano 저장/종료, Ctrl+D 파일 입력·Ctrl+Z/C 작업 제어, 가상 컨테이너 셸 진입/종료 검사.
- Docker 사용 중 삭제 거부, 이미지 pull 후 기존 컨테이너가 자동 갱신되지 않음, stop/start 후 파일 유지, commit/save/load 후 새 컨테이너에 파일 복원, --rm은 컨테이너만 정리하는 동작 검사.
- 고정된 안전한 명령 9개 조합을 실제 bash/GNU와 비교해 stdout/stderr/종료 코드 일치 확인. 참조 명령은 테스트 전용 임시 폴더에서만 실행하며 앱이 학습자 명령을 실제 bash로 실행하는 것은 아닙니다.
- subprocess와 네트워크 소켓을 사용하면 실패하도록 모킹한 상태에서도 Docker 및 압축 과제 통과. 전체 GNU/Docker 구현과 같다는 보장은 아닙니다.
- 활용·랜덤·종합 테스트에서 왼쪽 명령어/단원명을 숨기고 배우기·예시에서 복원. 단계·난이도·완료 기록·명시적 F1/F3은 유지.
- 실제 Qt 입력 → 시뮬레이터 → 미완료 두 번 → 이어서 조작 → 통과 → 진도 저장 흐름, 종료·기억노트·스크롤 회귀 유지.
- 1220×880 offscreen 앱 화면에서 테스트 목록 숨김·배우기 복원·한글·터미널·긴 결과 스크롤·버튼 배치 확인.

```sh
QT_QPA_PLATFORM=offscreen PYTHONPATH=.runtime:. python3 -m unittest test_simulator test_native_ui test_terminal_scrollback test_memory_notes test_checkpoints test_path_practice test_shutdown -q
QT_QPA_PLATFORM=offscreen PYTHONPATH=.runtime:.:.. python3 -m unittest discover -q
```

지원하지 않는 옵션, 실제 패키지/이미지 아카이브와의 차이, 미완료된 강의 범위는 README.md와 MATERIAL_COVERAGE.md에 명시했습니다. 4.0에서 별도 VM 설치 계획은 취소했습니다.

## 3.6.1 미완료 채점 후 이어서 조작 (2026-09-15)

전체 70개 테스트가 실제 Docker 통합 모드에서 통과했습니다(158초, skip 없음). Linux 실행 파일 3.6.1 빌드 및 리소스 자체 검사도 통과했습니다. Windows exe 빌드와 Windows 실기기 검사는 수행하지 않았습니다.

- 마우스로 채점한 뒤 입력 초점을 터미널로 복원. 예시·활용·랜덤·종합 복습에서 미완료여도 F5 재채점 가능, F6 건너뛰기는 불가.
- 채점 오류 시에도 실습을 초기화하지 않고 연결된 터미널로 입력 초점 복원.
- 40개 결과를 넣어도 결과 패널 높이는 130px 이하이며 내부 스크롤로 읽기 가능. 1220×880 offscreen 렌더에서 한글, 입력 커서와 터미널·버튼 표시 확인.
- 실제 Linux 셸에서 미완료 채점 두 번 후 동일 컨테이너·PTY 유지, 현재 경로·파일·환경변수 보존, 곧바로 추가 명령 입력 후 통과 및 학습 흐름 진행 검증.
- 기존 26단원·5종합 복습·완료 진도·기억노트·스크롤·종료·격리 검사 유지. 실습 이미지 3.3은 변경하지 않음.

강의자료 대조는 `MATERIAL_COVERAGE.md`에 기록했습니다. **분석 문서이며 새 관리자·Docker·자율주행 확장 과정을 구현하거나 검증한 것은 아닙니다.**

```sh
SHELLGROUND_INTEGRATION=1 QT_QPA_PLATFORM=offscreen PYTHONPATH=.runtime:. /usr/bin/python3 -m unittest test_checkpoints test_terminal_scrollback test_path_practice test_memory_notes test_backend test_shutdown test_real_lab test_native_ui -q
```

## 3.6.0 5개 단원마다 종합 복습 (2026-09-15)

최종 전체 68개 테스트가 Docker 통합 모드에서 모두 통과했습니다(149초, skip 없음).

Linux 3.6.0 실행 파일 빌드와 리소스 자체 검사를 통과했습니다. 기존 26개 단원에 종합 복습 5개를 별도 삽입했습니다. 실습 이미지 3.3은 그대로 사용하며 Windows exe 빌드·실기기 검증은 이번에도 수행하지 않았습니다.

- 05·10·15·20·25 뒤 목록 삽입, 기존 단원 번호 보존, 구간 완료 후 테스트 연결 및 미통과 시 다음 미완료 단원 잠금.
- F3은 해당 다섯 단원 설명, F1은 명시적 힌트. 자동 풀이·편집기 조작 안내는 테스트 화면에 표시하지 않음.
- schema 3 진도에 선택적 checkpoints 목록 추가. 기존 completed 기록 보존, 미완료 복습 재개, 완료 복습 재응시 검사.
- 5개 과제 × 3개 seed의 실제 Linux 실행 검증: 초기·부분 상태 거부, 완성 결과 통과, 필수 결과 삭제 후 다시 거부. nano의 실제 키 입력·저장·종료도 포함.
- 생성된 종합 과제에 아직 배우지 않은 명령이 없는지 검사하고 1220×880 offscreen 화면에서 목록·테스트 준비 화면을 확인.

```sh
SHELLGROUND_INTEGRATION=1 QT_QPA_PLATFORM=offscreen PYTHONPATH=.runtime:. /usr/bin/python3 -m unittest test_checkpoints test_terminal_scrollback test_path_practice test_memory_notes test_backend test_shutdown test_real_lab test_native_ui -v
```

## 3.5.1 터미널 스크롤 위치 유지 (2026-09-15)

전체 60개 테스트가 Docker 통합 모드에서 모두 통과했습니다(132초, skip 없음). Linux 3.5.1 실행 파일 빌드 및 리소스 자체 검사도 통과했습니다.

출력 파서의 실제 화면과 기록 열람 위치를 분리했습니다. 기록을 읽는 동안 새 출력·커서 표시 변경이 도착해도 동일한 줄을 유지합니다. 맨 아래에서만 출력을 따라가며, Shift+End/우클릭 또는 실제 입력 시 최신 화면으로 돌아갑니다.

- 스크롤 회귀 검사 6개: 연속 출력·제어 시퀀스 중 위치 유지, 기록 선택·복사, Shift+PageUp/PageDown·Shift+End, 일반/한글/붙여넣기 입력 복귀, 3,000줄 제한·오래된 줄 폐기, 초기화·기록 삭제, 창 크기 변경·터치패드 입력.
- 스크롤은 파서 화면을 과거 페이지로 바꾸지 않으므로 실제 Linux 입력·출력 처리는 계속 최신 상태로 유지됩니다.
- 학습 내용, 진도·기억노트 형식, 실습 이미지 3.3은 변경하지 않았습니다. Windows 실기기·exe 빌드는 이번에도 수행하지 않았습니다.

```sh
SHELLGROUND_INTEGRATION=1 QT_QPA_PLATFORM=offscreen PYTHONPATH=.runtime:. /usr/bin/python3 -m unittest test_terminal_scrollback test_path_practice test_memory_notes test_backend test_shutdown test_real_lab test_native_ui -v
```

## 3.5.0 상황별 절대·상대경로 실습 (2026-09-15)

최종 전체 54개 테스트가 Docker 통합 모드에서 모두 통과했습니다(122초, skip 없음).

Linux 3.5.0 실행 파일 빌드와 리소스 자체 검사를 통과했습니다. Windows용 코드는 POSIX 경로 계산을 사용하지만 Windows 실행 파일 빌드·실기기 검증은 수행하지 않았습니다. 실습 이미지 3.3과 진도 저장 형식은 변경하지 않았습니다.

- 26개 단원 및 기존 옵션·활용 조건을 유지하면서 현재/인접/먼 작업 위치를 생성합니다. 가까운 파일은 상대경로, 먼 곳에서 반복 작업은 이동 후 처리하도록 예시를 표시합니다.
- 26단원 × 12개 seed × 3개 유형의 명령을 검사해 각 실행 시점의 경로가 원래 목표를 가리키는지 확인합니다. 공백 인용·파이프·리다이렉션·./스크립트 실행 및 find의 절대경로 출력 조건도 검사합니다.
- 실제 Linux에서 인접/먼 위치의 파일 작업·목록·다운로드·압축·스크립트·패키지 문제 28개 조합과 절대/상대경로 대체 답안을 검사합니다.
- 첫 통합 실행은 샌드박스의 Docker 소켓 접근 제한으로 실패해, Docker 접근 승인을 받아 다시 실행했습니다.

```sh
SHELLGROUND_INTEGRATION=1 QT_QPA_PLATFORM=offscreen PYTHONPATH=.runtime:. /usr/bin/python3 -m unittest test_path_practice test_memory_notes test_backend test_shutdown test_real_lab test_native_ui -v
```

## 3.4.1 평가 화면 안내 정리 (2026-09-15)

Linux 3.4.1 실행 파일 재빌드 및 리소스 자체 검사를 통과했습니다.

활용 1·2와 올랜덤의 문제 안내에서 nano 조작 순서를 숨겼습니다. 배우기·예시와 사용자가 직접 여는 힌트·옵션 설명은 유지합니다. 입문 목록 및 경로 비교 설명에서 불필요한 저장 불필요 문구를 삭제했으며, 편집 문제의 저장 조건과 읽기 문제의 원본 보존 조건은 유지합니다.

선택한 회귀 검사 35개 중 33개 통과, Docker가 필요한 2개는 건너뛰었습니다. 이번 변경은 안내 표시와 문구에 한정하며 Docker 통합 검사는 다시 실행하지 않았습니다. Windows 실행 파일 빌드와 실기기 검증도 수행하지 않았습니다.

- Qt 화면에서 배우기·예시·활용·올랜덤 및 두 편집 문제 변형을 검사: 평가 안내에 조작 키와 풀이가 없고, 목표·시작 위치 및 명시적 힌트는 유지.
- 입문 설명·힌트·문제 문구 회귀 검사, 기존 학습 순서·진도 저장·기억노트·종료·단축키 검사 통과.

```sh
QT_QPA_PLATFORM=offscreen PYTHONPATH=.runtime:. /usr/bin/python3 -m unittest test_memory_notes test_backend test_shutdown test_real_lab.CurriculumTests test_native_ui -v
```

## 3.4.0 내 기억노트 (2026-09-14)

기억노트 검사 8개를 추가해 최종 전체 45개 테스트가 Docker 통합 모드에서 통과했습니다(skip 없음). Linux 3.4.0 실행 파일 빌드와 리소스 자체 검사도 통과했습니다. Windows 실기기 검증 범위는 변경되지 않았습니다.

- F7 규칙·예시 추천 및 설명 선택 텍스트 보존, F8 목록 열기, 터미널 초점에서 단축키 동작.
- Ctrl+S 저장, 재생성한 저장소·노트 창에서 복원, 제목·내용 검색, 중복 저장 방지, 수정과 삭제.
- 닫기·다른 노트 이동 시 저장/취소 확인, 삭제 확인, 저장 실패 시 미저장 내용 유지.
- 손상된 원본 파일 덮어쓰기 방지, 원자적 교체 실패 시 기존 노트 보존.
- 기억노트 JSON과 완료 진도 JSON 분리, 작업 중 단축키 제한, 한글 노트 화면 offscreen 렌더 확인.

```sh
SHELLGROUND_INTEGRATION=1 QT_QPA_PLATFORM=offscreen python3 -m unittest test_memory_notes test_backend test_shutdown test_real_lab test_native_ui -v
```

## 3.3.0 학습 순서 재구성·조합 과제·설명 화면 실습 (2026-09-14)

최종 전체 37개 테스트가 Docker 통합 모드에서 통과했습니다(skip 없음, 107초). Linux 3.3.0 실행 파일 빌드 및 리소스 자체 검사도 통과했습니다.

- 기존 15개 단원 키와 상세 옵션 설명을 유지하면서 기본 작업을 분리해 26개 단원으로 재배치했습니다. 명령 도입 순서 검사로 예시·활용 1·활용 2에서 아직 소개하지 않은 명령을 사용하는 회귀를 방지합니다.
- 폴더·파일 생성, cat 읽기, 실제 GNU nano 수정·저장, 개별 cp/mv/rm, 실행 권한 단원을 추가했습니다. 기본 화면은 짧은 설명만 표시하고 F3 전체 참고 내용을 유지합니다.
- 화면 실습은 pwd/ls/cat의 실제 PTY 출력으로 검사합니다. 이 출력은 현재 문제의 메모리에만 보관하며 진도 JSON에 저장하지 않습니다.
- 활용 2는 26개 단원마다 조합·복구 조건을 정의합니다. 같은 seed의 활용 1과 비교해 목표가 달라지는지, 실제 해결 결과가 통과하는지, 기본 풀이 반복으로 조합 과제가 통과하지 않는지 검사합니다.
- nano에서 수정하지 않은 상태와 저장 전 상태 거부, 실제 키 입력으로 한 줄 수정 및 기존 설정 보존·세 줄 저장을 검사합니다.
- F4로 설명을 유지한 자유 연습 시작, F4 재입력 시 초기화 방지, 자유 연습 채점/완료 처리 차단, F6 예시 전환을 Qt 테스트와 실제 Linux 학습 흐름에서 확인합니다.
- 종료·진도 보존, 한글 렌더링, Docker 격리, Linux PTY, 파일 형식별 다운로드 검사를 유지합니다. 1220×880 offscreen 화면에서 안내·버튼 표시도 확인했습니다.

```sh
docker build -t shellground-lab:3.3 lab
SHELLGROUND_INTEGRATION=1 QT_QPA_PLATFORM=offscreen python3 -m unittest test_backend test_shutdown test_real_lab test_native_ui -v
```

Linux 실행 파일 버전은 3.3.0입니다. Windows Hyper-V 실기기 및 Windows exe 빌드는 여전히 미검증입니다. 전체 옵션 설명이 모두 독립된 채점 문제인 것은 아닙니다.

## 3.1.1 완료 진도 자동 저장 표시 (2026-09-14)

Docker 통합 검사를 포함한 전체 27개 테스트 통과(skip 없음). Linux 실행 파일을 3.1.1로 다시 빌드했습니다. Windows 실기기 검증 범위는 변경되지 않았습니다.

단원 목록에서 완료/미완료를 명시하고 상단에 완료 진도 자동 저장 안내를 표시합니다. 저장 성공과 실패를 구분하며 기존 `schema: 3, completed: [...]` 저장 형식을 유지합니다. 명령 입력·터미널 화면·실습 파일은 저장하지 않습니다.

Qt 검사에서 예시만 통과/활용 1개 통과 상태가 미완료로 남고, 활용 2개 통과 시 즉시 완료 기록만 저장되는 것을 확인했습니다. 별도 프로세스의 실제 Qt 종료 이벤트 뒤 새 창을 만들어 완료/미완료 표시 복원, 첫 미완료 단원 선택, 빈 터미널 복원을 확인했습니다. 저장 실패 경고와 재시도 성공도 검사합니다.

## 3.1.0 옵션 교육·단축키·Windows 비 WSL 정책 (2026-09-14)

Linux에서 `test_backend`, `test_shutdown`, `test_real_lab`, `test_native_ui` 총 **25개 테스트 통과**, Docker 통합 검사 포함, skip 없음.

- 15개 단원: pwd 링크의 논리/물리 경로 보고서 및 ls -A/-alh/-1S 보고서 추가. 미해결 상태 거부·실제 해답 통과 확인.
- pwd -L/-P를 뒤바꾼 보고서, ls -a/-A 혼동, -h 누락, 크기 정렬 역순을 거부. ls 비교 문제는 3개 seed로 검사.
- Qt 터미널 초점에서 F1/F2/F3/F5/F6 작동, 비활성·작업 중 동작 방지, 미통과 단계 건너뛰기 방지, 다시 시작 취소/승인 확인.
- 단원 추가·재배치 후 기존 완료 기록 보존 및 새 미완료 단원 안내.
- Windows 정책은 Docker 정보 모킹으로 검사: LinuxKit 허용, WSL/Microsoft/알 수 없는 커널 및 Windows containers 차단. WSL에서 이미지 빌드·실습 시작 호출이 진행되지 않음.
- 실제 Linux PTY·파일 결과·다운로드·격리·정상 종료 회귀 검사 유지.

```sh
docker build -t shellground-lab:3.1 lab
SHELLGROUND_INTEGRATION=1 QT_QPA_PLATFORM=offscreen python3 -m unittest test_backend test_shutdown test_real_lab test_native_ui -v
```

Windows Hyper-V 설치·실행 및 Windows exe 빌드는 **실기기 미검증**입니다. LinuxKit 식별 정책은 지원 환경 확인이며 임의로 변조된 서버를 판별하는 보안 장치가 아닙니다. Home/ARM Windows와 미확인 커널은 지원하지 않습니다. 전체 옵션 설명이 모두 독립된 채점 문제로 구현된 것은 아닙니다.

## 3.0.1 종료 수정 (2026-09-14)

정리 완료 후 Qt의 후속 종료 이벤트를 다시 거부해 정리 작업을 반복하던 오류를 수정했습니다. 종료 중과 정리 완료 상태를 구분하고, 정리 실패 시 오류를 표시하며 재시도를 허용합니다.

`test_shutdown` 4개를 추가했습니다: 정리 후 정상 종료, 종료 연속 클릭 시 중복 방지, 정리 실패 시 반복 방지, 실제 실습 컨테이너 삭제 후 앱 종료. 기존 검사와 함께 Docker 통합 모드에서 **17개 테스트가 통과**했습니다.

```sh
SHELLGROUND_INTEGRATION=1 QT_QPA_PLATFORM=offscreen python3 -m unittest test_shutdown test_real_lab test_native_ui -v
```

## 3.0.0 초기 검증 (2026-09-10)

Ubuntu 24.04 x86-64, Python 3.12, Docker 29.1.3에서 검사.

- 13개 단원의 미해결 상태 거부와 실제 파일 결과 성공 검사 (현재 위치 문제는 PTY 검사에 포함).
- 옵션 순서 변경, 상대/절대 경로, 재귀·숨김 옵션 누락, 원본 삭제 후 불완전한 목록 거부.
- 실제 deb/sh/zip/tar.gz HTTP 다운로드 및 잘못된 저장 이름·손상 내용 거부.
- 압축 해제, Debian 패키지 추출, 권한과 스크립트 결과, 로그 필터, 파이프, 검색 결과 검사.
- PTY의 경로 유지, Tab 자동 완성, 위 화살표 기록, 오류 출력, Ctrl+C, 터미널 크기 변경.
- 컨테이너 비관리자 실행, 읽기 전용 루트, 네트워크 차단, 호스트 bind mount 없음.
- Qt 한글 글리프, ANSI와 한글 셀 너비, 키 전달, bracketed paste, 진도 잠금과 저장, 오류 복구.
- 실제 Qt 화면 위젯 → Linux → 채점 → 활용 문제 2개 → 진도 저장의 연동 검사.

`test_real_lab` 8개 + `test_native_ui` 5개 = 13개 테스트. 일부 테스트는 여러 단원·파일 형식·명령 변형을 반복 검사합니다. Qt 검사는 offscreen 테스트로 수행하며 사용자의 실제 데스크톱 화면을 캡처하지 않습니다.

## 미검증 / 제한

- Windows 실기기 빌드 및 실행: 미검증. Windows exe 산출물은 이 Linux 환경에서 생성하지 않았습니다. `build-windows.ps1`과 공용 소스 제공.
- 다른 Linux 배포판, 모든 xterm 기능, 모든 Linux 명령/옵션: 보장하지 않음.
- 외부 인터넷 다운로드와 관리자/커널/하드웨어 관리: 실습 범위에서 제외.
- 컨테이너는 가상 머신과 동일한 보안 경계가 아님. 개인 파일을 연결하지 않으며 악성 코드 분석 용도가 아님.
